<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk.dao.HouseDao">

    <select id="findUserPage" parameterType="map" resultType="com.jk.pojo.House1">
        select * from c_house
        limit #{startIndex},#{endIndex}
    </select>

</mapper>